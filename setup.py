"""Setup script for company-credit-rating package."""
from setuptools import setup, find_packages

setup(
    name="company-credit-rating",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.11",
)
